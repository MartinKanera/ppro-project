services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=database'
      - 'MYSQL_USER=user'
      - 'MYSQL_PASSWORD=supersecretpassword1'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
    ports:
      - '3306:3306'